# =============================================================================
# UBIRT — Environment variables reference
# Copy to `.env.local` for local dev. Set the same keys on Vercel for production.
# Never commit `.env.local` (it is gitignored).
# =============================================================================

# -----------------------------------------------------------------------------
# APP MODE
# -----------------------------------------------------------------------------
# mock = demo data when Supabase is not configured | live = automatic when VITE_SUPABASE_* are set
# VITE_DATA_PROVIDER is optional legacy flag; the app uses Supabase whenever URL + anon key exist.
VITE_DATA_PROVIDER=live

# Public site URL (auth redirects, email links). e.g. http://localhost:5173 or https://your-app.vercel.app
# For mobile builds (npm run build:mobile), use your production URL so /api/* routes work in the native app.
VITE_APP_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# SUPABASE (required for live mode) — https://supabase.com
# Dashboard → Project Settings → API
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=

# Server-only. Dashboard → API → service_role (never expose to browser / VITE_)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_URL=https://your-project.supabase.co

# Optional: use Resend as Supabase Auth SMTP (Authentication → SMTP Settings)
# Host: smtp.resend.com | User: resend | Password: your RESEND_API_KEY

# -----------------------------------------------------------------------------
# OPENAI (AI chat) — https://platform.openai.com/api-keys
# Server-only. Used by api/ai/chat.js on Vercel / `npx vercel dev`
# -----------------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# RESEND (transactional email) — https://resend.com/api-keys
# Welcome, password reset (custom routes), notifications
# Server-only. Verify domain or use onboarding@resend.dev for testing
# -----------------------------------------------------------------------------
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev
RESEND_FROM_NAME=UBIRT

# -----------------------------------------------------------------------------
# MUX (video streaming, optional) — https://dashboard.mux.com/settings/access-tokens
# Server-only. Used by api/video/ingest.js
# -----------------------------------------------------------------------------
MUX_TOKEN_ID=
MUX_TOKEN_SECRET=

# -----------------------------------------------------------------------------
# PAYMENTS (wallet coin purchases) — multi-gateway
# Set VITE_PAYMENT_GATEWAY to the active provider: fincra | paystack
# -----------------------------------------------------------------------------
VITE_PAYMENT_GATEWAY=fincra
VITE_PAYMENT_CURRENCY=NGN
# Server-side default (should match VITE_PAYMENT_GATEWAY)
PAYMENT_GATEWAY=fincra

# Fincra — https://app.fincra.com/ → Settings → API keys
# Docs: https://docs.fincra.com/docs/checkout-redirect
# Sandbox: https://sandboxapi.fincra.com | Live: https://api.fincra.com
# Webhook URL: https://your-domain.com/api/webhooks/fincra  (event: charge.successful)
FINCRA_API_KEY=
FINCRA_PUBLIC_KEY=
FINCRA_BUSINESS_ID=
FINCRA_WEBHOOK_SECRET=
FINCRA_API_BASE_URL=https://sandboxapi.fincra.com
# Fincra requires HTTPS redirect URLs (localhost is rejected in sandbox)
FINCRA_REDIRECT_URL=https://www.app.ubirtai.site

# Paystack (optional second gateway) — https://dashboard.paystack.com/#/settings/developer
# Public key → client (VITE_). Secret key → server webhook only.
# Webhook URL: https://your-domain.com/api/webhooks/paystack  (event: charge.success)
# Set VITE_PAYMENT_GATEWAY=paystack to use Paystack instead of Fincra.
VITE_PAYSTACK_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
PAYSTACK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# SENTRY (error monitoring, optional) — https://sentry.io
# -----------------------------------------------------------------------------
VITE_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# -----------------------------------------------------------------------------
# POSTHOG (product analytics, optional) — https://posthog.com
# -----------------------------------------------------------------------------
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com

# -----------------------------------------------------------------------------
# PUSH DELIVERY (server-only)
# -----------------------------------------------------------------------------
# Firebase project (e.g. ubirtai). The web SDK snippet from Firebase Console is
# client-side only — server push needs the service account below.
# Firebase Console → Project settings → Service accounts → Generate new private key
FIREBASE_PROJECT_ID=ubirtai
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@ubirtai.iam.gserviceaccount.com
# Paste private_key from JSON; in Vercel use literal \n for newlines inside quotes
FIREBASE_PRIVATE_KEY=
#
# APNs token auth
APNS_TEAM_ID=
APNS_KEY_ID=
APNS_BUNDLE_ID=ubirtai.app
# Multi-line .p8 contents (escaped with \n in Vercel env var)
APNS_PRIVATE_KEY=
# Use "true" for development sandbox, "false" for production
APNS_USE_SANDBOX=true
#
# Optional: protect /api/webhooks/notification-push when using Supabase Database Webhooks
NOTIFICATION_PUSH_WEBHOOK_SECRET=
#
# DEPLOYMENT NOTES (no env vars in repo)
# -----------------------------------------------------------------------------
#
# Native OAuth redirect:
# - Web: keep VITE_APP_URL as your website URL (https://app.ubirtai.site)
# - iOS/Android OAuth callback registered in Supabase: ubirtai.app://login
#
# GitHub  — source control
# Vercel  — hosting + serverless API (import repo, paste env vars from this file)
# Domain  — optional custom domain on Vercel
